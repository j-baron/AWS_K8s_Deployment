pipeline {
    agent any

    stages {
        stage('Create AWS EC2 Instances') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Deploy K8s to AWS EC2 Instance') {
            steps {
                echo 'Hello World'
            }
        }

    }
}
